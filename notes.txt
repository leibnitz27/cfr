TODO:

Inner / inline objects
variable declarations
reverse generic type determinations (for x : )
static blocks vs static initialisers
lift static initialisers into declarations.
inner class imports are wrong.